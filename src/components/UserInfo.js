export class UserInfo {
	constructor({ titleSelector, descriptionSelector }) {
		this._titleElement = document.querySelector(nameSelector);
		this._descriptionElement = doucument.querySelector(descriptionSelector);
	} //end const
} //end class
